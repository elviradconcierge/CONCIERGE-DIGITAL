import { Bot } from "lucide-react";
import { SimplePage } from "../../components/common";

export const AISupportPage = () => {
  return (
    <SimplePage
      title="AI Support"
      message="AI Support system for guest assistance and hotel operations. Configure AI chatbots, manage knowledge base, and optimize automated responses."
      icon={Bot}
    />
  );
};

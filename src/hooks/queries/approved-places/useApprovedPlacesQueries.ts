/**
 * Approved Third Party Places Queries and Mutations
 *
 * @deprecated This file is maintained for backward compatibility only.
 * Please import from specific hook files or from './index' instead.
 *
 * @example
 * // Old way (still works):
 * import { useApprovedPlaces } from './useApprovedPlacesQueries';
 *
 * // New way (preferred):
 * import { useApprovedPlaces } from './approved-places';
 * // or
 * import { useApprovedPlaces } from './queries';
 */

// Re-export everything from the index for backward compatibility
export * from "./index";

# Supabase Configuration
VITE_SUPABASE_URL=your_project_url
VITE_SUPABASE_ANON_KEY=your_anon_key

# Google Places API (Optional - can also be stored in Supabase secrets table)
# Get your API key from: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_PLACES_API_KEY=your_google_places_api_key

# Amadeus API Credentials (Optional - can also be stored in Supabase secrets table)
# Get your credentials from: https://developers.amadeus.com/
# 1. Create an account or log in
# 2. Create a new app in the Self-Service section
# 3. Copy the API Key (Client ID) and API Secret (Client Secret)
# 4. Use the Test environment credentials for development
VITE_AMADEUS_CLIENT_ID=your_amadeus_client_id
VITE_AMADEUS_CLIENT_SECRET=your_amadeus_client_secret